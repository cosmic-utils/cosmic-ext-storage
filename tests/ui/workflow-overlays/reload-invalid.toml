schema_version = 2
id = "reload-invalid"
description = "An intentionally unsupported overlay used only to prove atomic reload rejection."

[backend]
id = "ui-scenario"
capabilities = ["partition"]

[world]
revision = 2
unsupported_reload_field = true
