schema_version = 2
id = "physical-partition-format"
description = "A one GiB empty GPT disk used for create-partition and format flows."

[backend]
id = "ui-scenario"
capabilities = ["partition", "filesystem"]

[world]
revision = 0

[[world.disks]]
id = "disk0"
device = "/dev/ui-disk0"
model = "Scenario Disk"
size_bytes = 1073741824
partition_table = "gpt"
