schema_version = 2
id = "reload-after"
description = "A valid workflow overlay that deterministically changes the disk model."

[backend]
id = "ui-scenario"
capabilities = ["partition"]

[world]
revision = 1

[[world.disks]]
id = "disk0"
device = "/dev/ui-disk0"
model = "After"
size_bytes = 1073741824
partition_table = "gpt"
