# Locked, single-output compositor configuration for the UI capability test.
# The runner supplies the headless backend, socket, locale, and renderer.
output * mode 1280x800
output * scale 1
output * bg #202020 solid_color

font pango:DejaVu Sans 10
focus_follows_mouse no
focus_on_window_activation focus
workspace_auto_back_and_forth no
